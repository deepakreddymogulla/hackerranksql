SELECT 
    CAST(ROUND(LONG_W,4) AS DECIMAL(10,4))
FROM
    STATION
WHERE
    CAST(ROUND(LAT_N,4) AS DECIMAL(10,4)) = 
    (SELECT TOP 1 CAST(ROUND(LAT_N,4) AS DECIMAL(10,4)) FROM STATION WHERE LAT_N < 137.2345 ORDER BY 1 DESC);