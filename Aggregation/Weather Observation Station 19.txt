SELECT 
    CAST(ROUND(SQRT(
SQUARE(CAST(ROUND((MAX(LONG_W))-(MAX(LAT_N)),4) AS DECIMAL(10,4))) 
                    + 
SQUARE(CAST(ROUND((MIN(LONG_W))-(MIN(LAT_N)),4) AS DECIMAL(10,4))))
              ,4) AS DECIMAL(10,4) )
FROM STATION;