SELECT A1 FROM (SELECT TOP 1 LEN(CITY) AS A2, CONCAT(CITY,' ', LEN(CITY)) AS A1 FROM STATION ORDER BY 1,2) A
UNION
SELECT B1 FROM (SELECT TOP 1 LEN(CITY) AS B2, CONCAT (CITY,' ', LEN(CITY)) AS B1 FROM STATION ORDER BY 1 DESC,2 DESC) B;